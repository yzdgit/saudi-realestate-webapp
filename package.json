{
  "name": "realestate-eda",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.5",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "bun --env-file=.env run --cwd app dev",
    "build": "bun --env-file=.env run --cwd app build",
    "start": "bun --env-file=.env run --cwd app start",
    "lint": "bun --env-file=.env run --cwd app lint",
    "typecheck": "bun --env-file=.env run --cwd app typecheck",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:db:reset": "supabase db reset",
    "supabase:migration:new": "supabase migration new",
    "supabase:functions:serve": "supabase functions serve"
  }
}
